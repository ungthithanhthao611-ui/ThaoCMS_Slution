@model List<CMS.Data.Entities.OrderDetail>

@{
    Layout = "_LayoutAdmin";
    ViewData["Title"] = "Chi tiết đơn hàng";
}

<div class="container mt-4">
    <div class="d-flex justify-content-between align-items-center mb-4">
        <h2 class="text-dark fw-bold">CHI TIẾT ĐƠN HÀNG</h2>
    </div>

    <table class="table table-bordered table-striped table-hover shadow-sm">
        <thead class="table-dark">
            <tr>
                <th style="width: 80px;">ID</th>
                <th>Mã đơn hàng (Order ID)</th>
                <th>Mã sản phẩm (Product ID)</th>
                <th>Số lượng</th>
                <th>Đơn giá</th>
                <th>Thành tiền</th>
            </tr>
        </thead>

        <tbody>
            @foreach (var item in Model)
            {
                var total = item.Quantity * item.UnitPrice;
                <tr>
                    <td><strong>@item.Id</strong></td>
                    <td><span class="badge bg-light text-dark border">#@item.OrderId</span></td>
                    <td><span class="badge bg-light text-secondary border">@item.ProductId</span></td>
                    <td class="fw-semibold">@item.Quantity</td>
                    <td class="text-muted">@item.UnitPrice.ToString("#,##0") VNĐ</td>
                    <td class="text-danger fw-bold">@total.ToString("#,##0") VNĐ</td>
                </tr>
            }
        </tbody>
    </table>

    @if (!Model.Any())
    {
        <div class="text-center text-muted my-5">
            <i class="bi bi-list-stars display-3"></i>
            <p class="mt-3">Hệ thống chưa có chi tiết đơn hàng nào.</p>
        </div>
    }
</div>