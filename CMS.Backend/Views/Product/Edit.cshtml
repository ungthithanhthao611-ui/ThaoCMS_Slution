@model CMS.Data.Entities.Product

@{
    Layout = "_LayoutAdmin";
    ViewData["Title"] = "Chỉnh sửa sản phẩm";
}

<div class="container mt-4">
    <div class="card shadow border-0">
        <div class="card-header bg-warning text-dark py-3">
            <h5 class="mb-0 fw-bold"><i class="bi bi-pencil-square"></i> CHỈNH SỬA SẢN PHẨM</h5>
        </div>
        <div class="card-body p-4">
            <form asp-action="Edit" method="post" enctype="multipart/form-data">
                <input type="hidden" asp-for="Id" />
                <div class="row">
                    <!-- Cột bên trái: Các thông tin mô tả cơ bản của sản phẩm -->
                    <div class="col-md-8">
                        <div class="mb-3">
                            <label class="form-label fw-bold">Tên sản phẩm:</label>
                            <input asp-for="Name" class="form-control form-control-lg" required />
                            <span asp-validation-for="Name" class="text-danger"></span>
                        </div>

                        <div class="mb-3">
                            <label class="form-label fw-bold">Mô tả chi tiết:</label>
                            <textarea asp-for="Description" class="form-control" rows="8"></textarea>
                            <span asp-validation-for="Description" class="text-danger"></span>
                        </div>
                    </div>

                    <!-- Cột bên phải: Giá, Số lượng, Loại sản phẩm và Ảnh đại diện -->
                    <div class="col-md-4">
                        <div class="mb-3">
                            <label class="form-label fw-bold">Loại sản phẩm:</label>
                            <select asp-for="CategoryProductId" class="form-select" asp-items="ViewBag.CategoryList" required></select>
                            <span asp-validation-for="CategoryProductId" class="text-danger"></span>
                        </div>

                        <div class="mb-3">
                            <label class="form-label fw-bold">Giá bán (VNĐ):</label>
                            <input asp-for="Price" type="number" step="1000" class="form-control" required />
                            <span asp-validation-for="Price" class="text-danger"></span>
                        </div>

                        <div class="mb-3">
                            <label class="form-label fw-bold">Giá khuyến mãi (VNĐ - bỏ trống nếu không sale):</label>
                            <input asp-for="SalePrice" type="number" step="1000" class="form-control" />
                            <span asp-validation-for="SalePrice" class="text-danger"></span>
                        </div>

                        <div class="mb-3">
                            <label class="form-label fw-bold">Số lượng kho:</label>
                            <input asp-for="StockQuantity" type="number" class="form-control" required />
                            <span asp-validation-for="StockQuantity" class="text-danger"></span>
                        </div>

                        <div class="mb-3">
                            <label class="form-label fw-bold">Đổi ảnh đại diện sản phẩm mới:</label>
                            <input type="file" name="uploadImage" class="form-control" accept="image/*" />
                            <small class="text-muted">Chọn tệp hình ảnh từ máy tính nếu muốn thay ảnh mới.</small>
                        </div>

                        <div class="mb-3">
                            <label class="form-label fw-bold">Hoặc nhập link hình ảnh mới (URL):</label>
                            <input asp-for="ImageUrl" class="form-control" placeholder="https://..." />
                            <small class="text-muted">Nếu không tải ảnh lên, bạn có thể dán liên kết URL ảnh trực tiếp vào đây.</small>
                        </div>

                        @if (!string.IsNullOrEmpty(Model.ImageUrl))
                        {
                            <div class="mb-3 text-center">
                                <label class="form-label fw-bold d-block text-start">Hình ảnh hiện tại:</label>
                                <img src="@Model.ImageUrl" alt="Current Image" class="img-thumbnail" style="max-height: 120px; max-width: 180px; object-fit: cover;" onerror="this.onerror=null; this.src='https://placehold.co/180x120?text=No+Image'" />
                            </div>
                        }

                        <hr />
                        <div class="d-grid gap-2">
                            <button type="submit" class="btn btn-warning btn-lg fw-semibold text-dark">
                                <i class="bi bi-check-circle"></i> Cập nhật thay đổi
                            </button>
                            <a asp-action="Index" class="btn btn-secondary">
                                <i class="bi bi-arrow-left"></i> Quay lại danh sách
                            </a>
                        </div>
                    </div>
                </div>
            </form>
        </div>
    </div>
</div>
